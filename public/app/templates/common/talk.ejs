<li class="event-schedule-item">
  <h2 class="speech-time"><%= talk.time %></h2>
  <img class="speaker-photo" src="<%= talk.imagePath %>" alt="<%= talk.speakers.map(speaker => speaker.name).join(', ') %> photo" >

  <h3><%= talk.title %></h3>

  <p class="speaker-name"><%= talk.speakers.map(speaker => speaker.name).join(', ') %></p>
  <p><%- talk.description %></p>
</li>
<% if (talk.break) { %>
  <li class="event-schedule-item">
    <h2 class="speech-time"><%= talk.break.time %></h2>
    <h3><i class="fa fa-coffee" aria-hidden="true"></i> Coffee break</h3>
  </li>
<% } %>
<% if (talk.party) { %>
<li class="event-schedule-item">
  <h2 class="speech-time"><%= talk.party.time %></h2>
  <img class="speaker-photo" src="images/bottles.jpg">
  <h3><%= talk.party.title %></h3>
  <p><%- talk.party.description %></p>
</li>
<% } %>
